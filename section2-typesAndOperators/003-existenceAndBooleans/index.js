var a;

// goes to the internet and looks for a value

a = 0;

//if statements coerce a boolean value
//logs Something is there.
if (a || a === 0) {
  console.log('Something is there.')
}
//without or statement, nothing would be logged because 0 is coerced into a false value



